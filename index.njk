---
title: "All Cars"
layout: "layout.njk"
---

<div class="search-wrapper">
  <input type="text" id="searchInput" placeholder="Search for cars (e.g., Toyota, SUV)...">
</div>

<div class="pagination-controls">
  <button id="prevButton" disabled>← Previous</button>
  <span id="pageInfo">Page 1 of 9</span>
  <button id="nextButton">Next →</button>
</div>

<div class="card-grid" id="carGrid">
  {# The Nunjucks loop remains the same. We still generate all 100 cards. #}
  {% for car in cars.cars %}
    <a href="/cars/{{ car.id }}/" class="card" data-search-term="{{ car.make | lower }} {{ car.model | lower }} {{ car.year }}">
      <img src="{{ car.image_url }}" alt="{{ car.make }} {{ car.model }}">
      <div class="card-content">
        <h2>{{ car.year }} {{ car.make }} {{ car.model }}</h2>
        <p>{{ car.short_description }}</p>
        <span class="price">${{ car.price.toLocaleString() }}</span>
      </div>
    </a>
  {% endfor %}
</div>